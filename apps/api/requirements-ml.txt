# Optional ML dependencies for model-backed adapters.
# The core hackathon demo works without these packages.
#
# Install only the adapters you need:
#   pip install -r requirements-ml.txt        # everything (may take time)
#   pip install scikit-learn faiss-cpu         # batch clustering
#   pip install sentence-transformers          # semantic embedding
#   pip install transformers torch             # RoBERTa WHY/WHAT
#   pip install networkx                       # doc circularity graph
#   pip install tree-sitter                    # code AST parsing
#
# On Python 3.13, install versions with available wheels or use Python 3.11.

# --- Batch clustering ---
scikit-learn>=1.4.0
faiss-cpu>=1.7.4

# --- Semantic embedding ---
sentence-transformers>=2.2.0

# --- RoBERTa WHY/WHAT classifier ---
transformers>=4.40.0
torch>=2.2.0

# --- Documentation circularity graph analysis ---
networkx>=3.1

# --- Code AST parsing ---
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0
tree-sitter-javascript>=0.21.0
tree-sitter-typescript>=0.21.0

# --- Supabase (opt-in telemetry) ---
supabase>=2.3.0

# --- GitHub OAuth ---
httpx>=0.27.0
PyJWT>=2.8.0
